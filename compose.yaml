services:
  finance-journal:
    build:
      context: .
    image: finance-journal:local
    environment:
      TUSHARE_TOKEN: ${TUSHARE_TOKEN:-}
      TS_TOKEN: ${TS_TOKEN:-}
    volumes:
      - finance-journal-data:/app/_runtime
    command: ["--help"]

volumes:
  finance-journal-data:
